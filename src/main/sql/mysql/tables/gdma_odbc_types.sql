CREATE TABLE GDMA2_ODBC_TYPES
(
  id                INT not null,
  name              VARCHAR(255) NOT NULL,
  select_get_tables VARCHAR(1000) NOT NULL,
  connectionclass   VARCHAR(500) NOT NULL
);
